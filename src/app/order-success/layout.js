export const metadata = {
  title: "Order Successful | Lalbaug Roti House",
  description: "Your order has been placed successfully.",
};

export default function Layout({ children }) {
  return children;
}
